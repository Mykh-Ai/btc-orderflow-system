# Next research hypotheses — no current Snapshot V2 changes

This file records hypotheses from the post-unblinding audit. None is an implemented feature, validated predictor, or reason to re-score the 19 cases. A new experiment needs a newly specified cohort and immutable pre-outcome freeze before labels are opened. The present frozen Snapshot V2 and both predictor prompts remain unchanged.

| Hypothesis | Why present evidence is insufficient | Exact new pre-outcome evidence required | Source status and bounded new test |
| --- | --- | --- | --- |
| H1: Earlier directional Delta and late OI movement jointly help distinguish SL-before-TP1 from TP1 reachability. | T−240→T−60 Delta is positive in 4/9 negatives vs 8/10 reachers, and final OI declines in 8/9 vs 6/10. Strong counterexamples and 19 cases prevent a rule. | The **same** five frozen segment Delta percent, price percent, volume, and OI fields, plus side and cutoff, for a new chronology-valid trade cohort; no extra current-case transformation. | `snapshot_v2.py::_window` already derives Delta from `BuyQty−SellQty`, quantity from `TotalQty`, and OI difference from `OpenInterest` in the aggregated feed. Pre-register signs/directions on a new cohort, freeze before outcome access, then compare Target A without fitting thresholds to these 19 cases. |
| H2: Verified interaction/acceptance state of the *same* opposing zone carries more information than selected-zone presence. | Every selected significant zone is INSIDE with zero distance; its acceptance/sweep fields are uniformly empty. Matched-registry acceptance varies but refers to a different zone chosen by overlap. Bar-overlap interaction counts mix zone width and age. | Stable selected-zone ID and registry ID, their boundary/overlap mapping, timestamped approach/sweep/acceptance/re-entry events strictly before cutoff, zone age and width, and source quality; no post-cutoff event. | The repository has `market_monitor.zone_registry` and `snapshot_v2.py::_zone_fact/_matched_registry/_recent_cleared`; it reconstructs registry facts from historical feed. This audit has **not** verified durable event history or identity equivalence for the old cases. Audit provenance first, then freeze a newly defined zone-lifecycle view on new trades and test both targets. |
| H3: The ordered path between entry, TP1, and TP2 matters more than a single selected zone relation. | TP2 BEYOND the selected zone still occurs in 2/6 strong trades. The current view gives one qualified zone and one liquidity item, so it does not describe the ordered sequence of boundaries between TP1 and TP2 or their independent history. | All qualified pre-cutoff opposing zone boundaries intersecting entry→TP2, stable IDs, distance in execution quote and R, their pre-cutoff lifecycle and directional liquidity, with explicit market/execution quote conversion provenance. | Zone registry and significant-zone builders exist in `snapshot_v2.py::_structure_frames`; this audit did **not** establish that an ordered historical inventory is durably stored for every old cutoff. Specify a new representation before collecting a new cohort; freeze it prospectively and evaluate Target B only after blind decisions are saved. |
| H4: Historical chronology and source-quality validation removes misleading timing evidence. | `EX_EN_1779900918` has a two-hour naive-time interpretation defect; three valid cases carry a degraded/recovered-feed flag. A numeric delay is not comparable until timestamp semantics are proven. | Source timezone contract, raw signal clock and timezone, trade-key epoch, executor fill UTC, bar cutoff UTC, feed completeness and quality by segment, all documented before outcome unblinding. | Existing raw signal, Executor, and feed sources supported the committed `CHRONOLOGY_AUDIT.md` for the excluded case. Run the same gate for a new cohort before freezing; reject unresolved timestamp mismatches from scored analysis. |

Priority: validate H1 on a genuinely new cohort using **existing** fields first. In parallel, audit H2/H3 provenance without editing the current frozen snapshots. If H1 does not replicate and H2/H3 have usable pre-outcome sources, define one new frozen representation and one pre-registered A/B evaluation. Do not optimize on these 19 known outcomes.
