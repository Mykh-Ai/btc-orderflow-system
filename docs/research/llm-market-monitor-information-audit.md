# Market Monitor information audit for entry assessment

Research-only, 2026-09-20. Source contracts: `HEAD:market_monitor/snapshot_builder_v39a.py`, `HEAD:market_monitor/snapshot_builder.py`, `HEAD:executor_mod/entry_snapshot.py`, and the read-only production verdict/feed replay. The 21 frozen files are **reconstructed research inputs**. The durable historical monitor on all 21 actual verdict records was `market_monitor_snapshot_v1`; one Sep 14 record also retained a V2 entry snapshot, but its historical monitor remained v1. No historical v39A model-input verdict is available.

| Needed fact | Status | Observed contract and gap |
|---|---|---|
| Exact 5/15/30/60/240/1440m closed windows | USEFUL | v39A emits expected/used rows, missing timestamps, duplicate count, quality, OHLC, delta and OI. All 21 were reconstructed with 1440/1440. Historical v1 current feed was day-scoped in two decisive examples. The v39A `readiness` implementation does not depend on `incomplete_windows`; callers must inspect every window. |
| 60m/240m price range position | MISSING | v39A emits high/low/close, but not `(close-low)/(high-low)`. Research input adds the exact formula as `research_derived_range_position` (60m and 240m); it is not a runtime v39A field. Base repaired 37E `close_position` describes its own broader state window, not both local windows. |
| Directional delta and delta_pct | USEFUL / AMBIGUOUS | v39A has each window's signed delta and delta_pct as a **fraction**. Base structure `metrics.delta_pct` uses percent points; simultaneous nested values can differ by 100× unless units are explicit. Direction-adjusted values should be separately named and sourced. |
| Open-interest change | USEFUL | v39A per-window first-to-last OI change exists. Trust/source and synthetic/recovered lineage should accompany it; a signed change is not itself bullish/bearish. |
| Current price versus VWAP/POC | AMBIGUOUS | Signal and legacy `market.context` may carry VWAP/POC, but v39A does not publish one authoritative current price comparison, horizon or source. Old prompt named `rolling_vwap_approx`; V2 does not explain its reliability. |
| Support/resistance distance | MISSING | v39A observed 240/1440 highs/lows and inherited v1 zone prices are available. The v1 builder calculates distance to sort zones but omits it from emitted zone items. There is no explicit directional nearest credible opposing-level distance with unit/quality. |
| Target room relative to opposing structure | MISSING | V2 `execution_geometry` can expose planned TP and R when known, but it does not relate them to an opposing zone. All 21 sanitized historical inputs have UNKNOWN planned geometry because pre-decision provenance was not provable. Never infer room from a zone label alone. |
| Significant market zones | USEFUL / AMBIGUOUS | v1 base emits near/above/below significant zones, significance and quality; v39A nests them in `liquidity_zones.significant_market_zones`. Scope may be day-limited in historical v1, and no explicit room/distance is emitted. |
| Liquidity zones | USEFUL / REDUNDANT | v1 liquidity buckets and significant zones can overlap, while v39A structure also repeats observed highs/lows. Deduplicate by zone identity/source and distinguish observed extrema from qualified liquidity. |
| Broad 1d/3d/7d/30d context | MISSING in v39A / AMBIGUOUS in v1 | Base v1 emits these windows, but v39A does not carry them directly. Research replay added base broad context with explicit coverage. Nine of 21 have incomplete 30d coverage; some v39A quality still says READY because it checks only current feed. Broad disagreement is risk/uncertainty, not automatic veto. |
| Data completeness and recovered/degraded status | USEFUL / AMBIGUOUS | v39A local windows expose row counts and recovered flag. It does not expose 1d/3d/7d/30d completeness. The feed adapter conservatively marks an entire file `RECOVERED_DEGRADED` when any row is synthetic, so row-level degradation is coarse. Three reconstructed current windows are tagged recovered and need sensitivity treatment. |
| Structure/bias with one meaning | CONTRADICTORY / REDUNDANT | v39A `market_state` may contain the base `market_structure_state`, while `state_memory.market_state`, observed `structure`, historical `market.context`, and zone labels also convey bias. The prompt receives no precedence or provenance rule; bearish expansion can still be read as “above support = bullish.” Separate descriptive structure state from candidate direction. |
| Upstream admission and planned geometry | MISSING in historic replay | V2 spends substantial text on A/B and geometry; all 21 sanitized research snapshots correctly mark filter provenance and planned geometry unknown. These should be sourced at the original decision boundary rather than reconstructed from later fill/order fields. |

## Concrete data/prompt separation

- `EX_EN_1785025567` (2026-07-26): historical v1 240m used 27/240 rows after UTC midnight; read-only replay used 240/240, delta_pct +0.075581, OI +34.90, range position 0.954854. Its recorded UNCLEAR cannot be attributed to prompt semantics alone.
- `EX_EN_1789351940` (2026-09-14): historical v1 240m used 133/240 rows; replay used 240/240, delta_pct -0.032166, OI +324.39, range position 1.0. The historical day-slice delta_pct was -0.008737. This is a material change in market evidence under the same cutoff.
- `EX_EN_1778813539`: verdict creation precedes the claimed cutoff by about 7161 seconds. The replay can produce pre-cutoff bars for that claimed cutoff, but those bars were not available when the verdict was recorded; classify INPUT_INSUFFICIENT.
- The one historical V2 `entry_snapshot` record was generated for the game after the trading decision path; it cannot prove that fill-derived geometry was known **before** entry. Frozen research inputs exclude actual fill, order, PnL and later lifecycle fields.

The old prompt’s late-entry and conflict concepts are decision questions. Defective windows and absent distances are data-contract problems. Adding stronger prose cannot establish the missing facts.
