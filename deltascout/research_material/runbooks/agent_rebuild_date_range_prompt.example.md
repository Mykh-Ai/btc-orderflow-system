# Local Runbook Template

Create a local-only file named `agent_rebuild_date_range_prompt.md` in this folder.

Typical private contents:

- how to access the server
- working directory and environment activation
- exact rebuild commands
- output sync steps
- validation checks

Do not commit the real runbook.
